# core 固有のADR

このリポ**内に閉じた**設計判断を置く（横断決定は `../docs-hub/docs/decisions/`）。

判定: リポを切り替えても言える決定・複数リポが参照する決定は docs-hub へ。core 内部の実装方針だけここへ。

- [ADR-001](ADR-001-counts-representation.md): 手牌の内部表現に枚数ベクトル(Counts)を採用する
